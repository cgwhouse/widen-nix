{
  catppuccin.sddm = {
    enable = true;
    flavor = "mocha";
    accent = "green";
  };
  
  catppuccin.plymouth = {
    enable = true;
    flavor = "mocha";
    # TODO: accent here too? probably not
    # accent = "green";
  };
}
